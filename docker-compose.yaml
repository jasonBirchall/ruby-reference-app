version: '3'
services:
  db:
    image: postgres
  rails-app:
    build: .
    command: ./start.sh
    volumes:
      - ./helloworld:/app
    ports:
      - "3000:3000"
    depends_on:
      - db
    environment:
      DATABASE_API_URL: postgres://postgres@db/rails-app_development
